{
  "operation_id": "archiveCard",
  "replay_safety": "unsafe_mutation",
  "cases": [{
    "input": { "cardId": "PLACEHOLDER" },
    "assertions": { "status": 200, "response_schema_valid": true }
  }]
}
