# Xiaohongshu (小红书)

## Overview
Chinese social media/lifestyle platform (Instagram + Pinterest hybrid). Archetype: Social Media.

## Operations
| Operation | Intent | Method | Notes |
|-----------|--------|--------|-------|
| searchNotes | search posts by keyword | Navigate to /search_result?keyword= | Extracts from Vue SSR state `__INITIAL_STATE__.search.feeds` |
| getNoteDetail | get post detail + comments | Navigate to /explore/{noteId} | Note from SSR state, comments intercepted from async API |
| getUserProfile | get user profile | Navigate to /user/profile/{userId} | Extracts from `__INITIAL_STATE__.user.userPageData`. May trigger CAPTCHA under rate limiting |
| getUserNotes | get user's posted notes | Navigate to /user/profile/{userId} | Extracts from `__INITIAL_STATE__.user.notes` (paginated array of pages). Same CAPTCHA risk as getUserProfile |
| getExploreFeed | get explore/discover feed | Navigate to /explore | Extracts from `__INITIAL_STATE__.feed.feeds`. Returns ~70 recommended notes |
| likeNote | like a note ✅ SAFE | Click `.engage-bar-style .like-wrapper` | Navigates to note detail, clicks like button. Idempotent (skips if already liked). Requires login |
| bookmarkNote | bookmark/collect a note ✅ SAFE | Click `.engage-bar-style .collect-wrapper` | Navigates to note detail, clicks collect button. Idempotent (skips if already bookmarked). Requires login |

## API Architecture
- Vue 3 SSR app — data baked into `window.__INITIAL_STATE__` on page load
- Internal REST APIs exist at `edith.xiaohongshu.com/api/sns/web/v1/*` and `/api/sns/web/v2/*`
- **Direct API calls fail (500)**: All endpoints require anti-bot signature headers (`X-s`, `X-t`, `X-s-common`) generated by obfuscated JavaScript from `as.xiaohongshu.com/api/sec/v1/scripting`
- Comments loaded async via `edith.xiaohongshu.com/api/sns/web/v2/comment/page` — intercepted from page's own JS
- Like/bookmark work by clicking DOM buttons — the page's own JS handles the signed API calls

## Auth
- Cookie session (`web_session` cookie)
- Search, note detail, and explore feed work without login
- User profile may require session or triggers CAPTCHA more aggressively
- Like and bookmark require login (cookie session)
- Anti-bot: custom obfuscated JS signing (not standard PerimeterX/DataDome/Akamai)

## Transport
- `page` (L3 adapter) — all data extracted from page navigation + SSR state + button clicks
- Cannot downgrade to `node` — API signing is mandatory and deeply obfuscated

## Extraction
- **Search**: `__INITIAL_STATE__.search.feeds._rawValue` (Vue ref) — array of note cards
- **Note detail**: `__INITIAL_STATE__.note.noteDetailMap[noteId].note` — nested under `.note` when SSR-hydrated
- **Comments**: Intercepted from `waitForResponse` on `/api/sns/web/v2/comment/page`
- **User profile**: `__INITIAL_STATE__.user.userPageData` — contains basicInfo, interactions, tags
- **User notes**: `__INITIAL_STATE__.user.notes` — paginated array of arrays, each item has `{id, noteCard, xsecToken}`
- **Explore feed**: `__INITIAL_STATE__.feed.feeds` — array of note cards, same structure as search
- **Like/bookmark**: Click button → intercept API response to confirm success, read SSR state for current status
- Vue refs: some state values wrapped in `{ _rawValue, _value, dep }` — access via `._rawValue`

## Known Issues
- **CAPTCHA on user profiles**: Rapid page navigation triggers CAPTCHA (`verifyType=124`). Rate-limit based, not auth-based. Affects both getUserProfile and getUserNotes. Resolve by solving CAPTCHA in browser or waiting.
- **Vue reactive wrappers**: State values may be plain objects or Vue refs depending on hydration timing. Adapter handles both via `?._rawValue ?? val` pattern.
- **xsecToken**: Note detail URLs require a `xsec_token` parameter for reliable access. Token comes from search results or explore feed.
- **Like/bookmark require login**: These operations need an active `web_session` cookie. Without login, the click will fail silently or redirect to login.
